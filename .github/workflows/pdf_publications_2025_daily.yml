name: Build PDF Publications (Venezuela Recent)

on:
  schedule:
    - cron: "0 10 * * *"
    - cron: "0 11 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  pdf-pubs:
    runs-on: ubuntu-latest
    steps:
      - name: Check 6 AM ET window
        id: et_window
        shell: bash
        run: |
          ET_HOUR="$(TZ=America/New_York date +%H)"
          if [ "$ET_HOUR" = "06" ]; then
            echo "run_update=true" >> "$GITHUB_OUTPUT"
          else
            echo "run_update=false" >> "$GITHUB_OUTPUT"
            echo "Skipping run outside 6 AM ET window (current ET hour: $ET_HOUR)."
          fi

      - uses: actions/checkout@v4
        if: github.event_name == 'workflow_dispatch' || steps.et_window.outputs.run_update == 'true'

      - name: Setup Python
        if: github.event_name == 'workflow_dispatch' || steps.et_window.outputs.run_update == 'true'
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install deps
        if: github.event_name == 'workflow_dispatch' || steps.et_window.outputs.run_update == 'true'
        run: |
          python -m pip install --upgrade pip
          pip install requests feedparser python-dateutil

      - name: Build PDF publications JSON
        if: github.event_name == 'workflow_dispatch' || steps.et_window.outputs.run_update == 'true'
        run: |
          python scripts/build_pdf_publications.py

      - name: Commit updates
        if: github.event_name == 'workflow_dispatch' || steps.et_window.outputs.run_update == 'true'
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@users.noreply.github.com"
          git add docs/data/pdf_publications_recent.json
          git commit -m "chore(data): update Venezuela recent PDF publications" || exit 0
          git push
